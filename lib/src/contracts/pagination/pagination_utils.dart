/// Classe utilitária apenas para resolver página atual e caminho atual.
/// Aqui você pode implementar a lógica de extrair do contexto, URL etc.
class PaginationUtils {
  static int resolveCurrentPage(String pageName) {
    // Retorne a página atual (por exemplo, 1, se não tiver nada)
    return 1;
  }

  static String resolveCurrentPath() {
    // Retorne o caminho atual, algo como '/'
    return '/';
  }
}